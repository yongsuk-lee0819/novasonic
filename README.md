# 노바소닉 (NOVASONIC) 상세페이지

17MHz 초고주파 홈케어 디바이스 노바소닉의 제품 상세페이지입니다.

## 구성

| 파일 | 설명 |
| --- | --- |
| `index.html` | 상세페이지 본문. 외부 라이브러리·폰트 의존성 없는 단일 파일 |
| `detail-0510.jpg` | 초기 이미지 버전. 현재는 SNS 공유 썸네일(OG 이미지)로만 사용 |

## 로컬에서 확인하기

브라우저로 `index.html`을 바로 열면 됩니다. 별도 빌드 과정이 없습니다.

```bash
# 또는 로컬 서버로 확인
python3 -m http.server 8000
# http://localhost:8000
```

## 배포

정적 파일 하나뿐이라 어떤 정적 호스팅에도 그대로 올릴 수 있습니다.

**GitHub Pages**

1. 저장소 `Settings` → `Pages` 이동
2. `Source`를 `Deploy from a branch`로 선택
3. 브랜치는 `main`, 폴더는 `/ (root)` 선택 후 저장
4. 1~2분 뒤 `https://yongsuk-lee0819.github.io/novasonic/` 에서 확인

**Netlify / Vercel**

저장소를 연결하면 됩니다. 빌드 명령은 비워두고, 배포 디렉터리는 저장소 루트로 지정합니다.

## 배포 전 확인 목록

페이지에서 분홍색 점선으로 표시된 `확인 필요` 항목과 `가안` 안내문은
실제 정보로 교체한 뒤 배포해야 합니다.

- [ ] 비교표의 "특징" 행 문구
- [ ] 사용 방법 4단계 — 제품 매뉴얼 기준으로 확정
- [ ] 제품 사양: 모델명, 크기·무게, 배터리·충전, 구성품, 제조원·수입원, 인증 정보, A/S 문의처
- [ ] 주의사항 — 제품 분류(의료기기 여부)에 맞춰 확정
- [ ] 확정 후 `.tbd` / `.draft-note` 요소와 관련 CSS 제거

> 효능·효과 표현은 제품 분류에 따라 광고 심의 대상입니다.
> 현재 본문에는 확인된 사실(동작 주파수 17MHz, 초당 진동수)만 기재되어 있습니다.

## 브랜드 색상

| 용도 | 값 |
| --- | --- |
| 메인 네이비 | `#22376b` |
| 진한 네이비 | `#1a2b55` |
| 포인트 코랄 | `#f0715a` |
